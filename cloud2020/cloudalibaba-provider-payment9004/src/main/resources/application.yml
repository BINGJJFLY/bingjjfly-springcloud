server:
  port: 9004

spring:
  application:
    name: nacos-payment-provider
  cloud:
    nacos:
      discovery:
        server-addr: 192.168.88.128:8848

management:
  endpoints:
    web:
      exposure:
        include: '*'